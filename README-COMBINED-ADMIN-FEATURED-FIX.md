# Combined Admin Actions + Featured Car Fix

This patch combines two updates:

1. Admin page actions enabled for Cars, Brands and Colours.
2. Landing page featured car now comes from the catalogue instead of a hard-coded image/description.

Apply this patch over a copy of the full Car-Rental-Angular repository. This zip contains changed files only, not the full Angular project.
