export interface RewardSettings {
  pointsPerPound: number;
  firstPaidBookingBonus: number;
  pointsPerTenPoundsDiscount: number;
  minimumRedeemablePoints: number;
  maximumRedemptionPercent: number;
  updatedAt: string;
}
