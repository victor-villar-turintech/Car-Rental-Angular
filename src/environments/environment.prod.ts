export const environment = {
  production: true,
  /** Toggle to true to use the NestJS backend. Off by default so the static demo build keeps working. */
  useBackend: false,
  /** Base URL of the NestJS backend. */
  backendUrl: '/api',
};
